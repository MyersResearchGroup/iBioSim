#!/bin/sh
#
# run-time wrapper for BioSim gui
export DYLD_LIBRARY_PATH=$ATACSGUI/lib:$DYLD_LIBRARY_PATH
CLASSPATH=$ATACSGUI/gui/dist/classes

for jarFile in $ATACSGUI/gui/lib/*.jar
do
    CLASSPATH=$CLASSPATH:$jarFile
done

exec java -Xmx512M -classpath $CLASSPATH -Dapple.laf.useScreenMenuBar=true -Xdock:name="ATACSGUI" -Xdock:icon=$ATACSGUI/gui/icons/ATACS.png main.Gui -atacs
