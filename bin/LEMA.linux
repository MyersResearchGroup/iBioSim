#!/bin/sh

export LD_LIBRARY_PATH=$LEMA/lib:$LD_LIBRARY_PATH
CLASSPATH=$LEMA/gui/dist/classes

for jarFile in $LEMA/gui/lib/*.jar
do
    CLASSPATH=$CLASSPATH:$jarFile
done

exec java -Xmx512M -classpath $CLASSPATH main.Gui -lema
